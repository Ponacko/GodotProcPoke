namespace ProcPoke.Data;

/// <summary>
/// The six permanent stats of the Gen 5 stat model. Order matches veekun stat ids 1–6, which is also
/// the canonical display order.
/// </summary>
public enum Stat
{
    Hp,
    Attack,
    Defense,
    SpecialAttack,
    SpecialDefense,
    Speed,
}
